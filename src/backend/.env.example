# Database credentials
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=password
DB_NAME=mydatabase

# JWT Secret for authentication
SECRET=mysecretkey

# API base Port for the backend
PORT=3000

# CORS setting
CORS_ORIGIN=http://localhost:3000
